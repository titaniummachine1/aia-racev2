racecar: round 5 - the wall-scrape fix (user: totalled like NASCAR). TWO
stacked bugs: (1) cross_y sign convention - Unity is LEFT-handed so cross_y>0
is a RIGHT turn; the mapping was inverted and aimed the OUTSIDE edge into the
wall (proved analytically before the edit). (2) the aim point sat ON the
corridor edge with zero margin - the car has width and scrapes. Fixed: KAPEX
+2.0 (handedness-correct = inside edge) and target clamped to the middle 50%
of the corridor (t in [0.25, 0.75], both live-tunable Float nodes). 81 nodes
/ 106 conns, validator-gated, deployed both load points.

