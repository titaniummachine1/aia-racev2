racecar: TitaniumV2 round 2 - THE BUGS THE USER FOUND: (1) ports resolved by
name only while math nodes carry in+out both named Float1 - wires landed
output-to-output and the car stood still; connect() now resolves by
(id, POLARITY) src=out dst=in with TYPE checking. (2) missing car properties
block (ConstructRacingV2Properties full 12-port dev pattern: name/color/
country/stats - speed 10 turn 10 health 0 per user rules). (3) clumped nodes -
layout now shelf-packs by each template sizeDelta collision bounds.
validate() gates every save: direction, types, required inputs wired (with
documented-optional allowlist). 52 nodes / 62 conns, deployed both load
points. TimePlot = one series (name+value).

