racecar: round 3 - evidence-driven crash fix + AST-style layered layout.
Crash decoded from the 21:59 timeplot (simTime 7.96s): v_tgt ALIVE 50->122.9
(severity brain works), speed hit 77.1 then collapsed to ~3 = STEERED into a
wall. Fixes: Autosteer aims NEXT centre (dev-proven; the P2 two-ahead aim
overshot corners), waypoint index inputs hard-wired non-null, wall brake from
the front ray (driver_config r=0.75 d=12 avoid 4.0) merged via max() as
(a+b-|a-b|)/2 (still zero compare-enum guesses), ray-B Debug to identify the
ray letter. LAYOUT: layered dataflow (topo columns + barycenter rows +
sizeDelta collision spacing) - 18x11 grid, wires run mostly forward.
Note: game timeplot JSON writes locale decimal commas - parse with the
digit,digit fixup.

