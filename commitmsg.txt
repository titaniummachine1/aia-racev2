racecar: TitaniumV2 = the analytical controller AS A GAME GRAPH (41 nodes /
50 wires, deployed to Saves/RacingV2 + data/graphs). Severity = 1-dot of
normalized waypoint segments (sign-invariant), v_tgt = clamp(FAST-severity*
GAIN), throttle/brake = pure float clamps (no enum guesses), Autosteer as v0
steer muscle toward P2 lookahead, ModularCarController direct drive,
TimePlot(v_tgt,speed)+Debug(brake) = observable assumptions. Plus
extract_templates.py (53 node templates + port/polarity maps mined from the
14 saves) + graph_builder.py (save emitter). Constants deduped (size =
solver speed). Sandbox files stamped NOT A SIMULATOR (user rule).

