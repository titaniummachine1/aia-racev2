racecar: round 4 - APEX RACING LINE from the waypoint corridor edges.
Decomp-pinned (Waypoint.GetLeftPoint/GetRightPoint/GetLateralEdgePoint/
ProjectExtentsOntoAxis): Left/Right = car-WIDTH-aware corridor edge points,
width to both sides = |L-C|/|R-C| derivable in-graph (Blockhead debug =
Waypoint.BuildDebugSnapshot, real). Graph: L/R edges of Next + turn-side pick
as pure float math (cross_y of the severity normals, target = L + (R-L) *
clamp(-cross*KAPEX,0,1)) -> Autosteer aims the INSIDE edge = the champions
Waypoint 1L/1R line. KAPEX = -2.0 Float node = live side-flip/tuning knob.
Ray map pinned from hints: RaycastHit2 = forward (wall brake correct);
HitInfo.Float1 = distance, infinity when clear (NaN-safe). 81 nodes / 106
conns, layered layout, validator-gated.

